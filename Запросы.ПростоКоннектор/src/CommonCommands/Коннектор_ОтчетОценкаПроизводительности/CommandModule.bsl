
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если Не ЕстьОтчетОценкаПроизводительности() Тогда
		Возврат;
	КонецЕсли;    
	
	ОткрытьФорму("Отчет.ОценкаПроизводительности.Форма");
	
КонецПроцедуры           

&НаСервере
Функция ЕстьОтчетОценкаПроизводительности()
	
	Возврат Метаданные.Отчеты.Найти("ОценкаПроизводительности") <> Неопределено;
	
КонецФункции
